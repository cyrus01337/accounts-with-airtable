# Accounts with Airtable
This is an exemplary NextJS project that aims to showcase the integration of auth using Airtable as the main database.

Passwords are hashed using Argon2 before sending a server request which persists within the database as well for maximum user security.
